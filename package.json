{
  "name": "markdown-search-extension",
  "displayName": "Markdown Search",
  "description": "A minimal VS Code extension foundation for a future markdown search tool.",
  "version": "0.1.0",
  "publisher": "plainionist",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:jekyllLens.openSearch"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "jekyllLens.openSearch",
        "title": "Jekyll Lens: Open Search"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "@types/vscode": "^1.95.0",
    "typescript": "^5.6.2"
  }
}